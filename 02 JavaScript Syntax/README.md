# 02 JavaScript Syntax

## The Grammar of JavaScript

Syntax is the set of rules that defines how a JavaScript program is constructed. Just like English has grammar rules (subject-verb-object), JavaScript has rules for how to write instructions that the computer understands.

## 📚 Topics

1.  **[01 Fixed Values](./01%20Fixed%20Values/index.html)**
    *   Literals: Numbers and Strings.
    *   How to write data directly.

2.  **[02 Variables](./02%20Variables/index.html)**
    *   `var`, `let`, and `const`.
    *   Storing data values.

3.  **03 Operators**
    *   Arithmetic (`+`, `-`, `*`, `/`).
    *   Assignment (`=`).

4.  **04 Expressions**
    *   Combinations of values, variables, and operators.
    *   Computing a single value.

5.  **05 Comments**
    *   Single line `//`.
    *   Multi-line `/* ... */`.
    *   Why we document code.

6.  **06 Identifiers**
    *   Naming rules (letters, digits, `_`, `$`).
    *   Case sensitivity.
    *   CamelCase convention.

---
*Open `index.html` to browse these topics interactively.*