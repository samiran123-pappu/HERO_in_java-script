# JavaScript Let

`let` was introduced in ES6 (2015).

## Rules
- block scoped
- cannot be redeclared in same scope
- can be reassigned
- temporal dead zone before declaration

## VIP Points
- Use `let` when value must change.
- Prefer block scope behavior over `var`.
