const mode = "switch";

if (mode === "if") {
  console.log("Use if for simple true/false checks.");
} else if (mode === "switch") {
  console.log("Use switch for many exact matches.");
} else {
  console.log("Use ternary for short expressions.");
}
