# JavaScript if

Use `if` to run code only when a condition is true.

## Syntax
```js
if (condition) {
  // code
}
```

## VIP Points
- `if` keyword must be lowercase.
- Condition should evaluate to `true` or `false`.
- Use braces for clarity, even with one line.
