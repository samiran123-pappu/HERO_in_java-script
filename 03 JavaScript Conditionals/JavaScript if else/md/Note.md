# JavaScript if else

Use `if...else` for two-way decisions.

## Syntax
```js
if (condition) {
  // true block
} else {
  // false block
}
```

Use `else if` to test extra conditions.

## VIP Points
- Order matters in `else if` chains.
- First true condition wins.
- End with `else` for fallback behavior.
