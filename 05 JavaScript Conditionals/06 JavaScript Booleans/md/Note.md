# JavaScript Booleans

Boolean values are only `true` or `false`.

They are the base of comparisons, conditions, and loops.

## VIP Points
- `true` and `false` must be lowercase.
- Many expressions evaluate to booleans.
- Avoid `new Boolean(...)`; use primitive booleans.
