# JavaScript Continue

`continue` skips current iteration and jumps to next iteration.

## Uses
- ignore specific values
- skip invalid data while iterating
- continue to labeled outer loops

## VIP Points
- `continue` does not exit loop; it skips current cycle only.
