# JavaScript Scope Reference

Quick recap:

- Global: visible everywhere in same script environment
- Function: visible only inside function
- Block: visible only inside `{}` for `let`/`const`
- Hoisting: declarations first, initializations later
- Strict mode: safer, stricter JavaScript
