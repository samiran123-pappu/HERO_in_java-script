# JavaScript Use Strict

`"use strict";` enables strict mode.

Benefits:

- Prevents accidental globals
- Turns silent mistakes into errors
- Disallows problematic syntax

You can apply strict mode:

- At script level
- Inside a function
