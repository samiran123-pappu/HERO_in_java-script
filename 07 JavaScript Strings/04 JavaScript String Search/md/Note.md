# JavaScript String Search

String search methods find text positions and matches.

## Methods
- `indexOf`, `lastIndexOf`, `search`
- `match`, `matchAll`
- `includes`, `startsWith`, `endsWith`

## VIP Points
- `indexOf` returns `-1` if not found.
- Many search methods are case-sensitive.
