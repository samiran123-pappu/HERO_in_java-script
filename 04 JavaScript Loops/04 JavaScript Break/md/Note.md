# JavaScript Break

`break` immediately exits a loop or switch.

## Uses
- stop loop early
- stop switch fall-through
- break labeled outer loops/blocks

## VIP Points
- `break` is critical inside `switch` cases.
- Labeled break is useful in nested loops.
