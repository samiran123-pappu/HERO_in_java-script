# OLD Condition Topics

Legacy folders expanded with detailed structure:
- `IF_ELSE`
- `TERNARY`
- `SWITCH-CASE`

Each folder now includes:
- `md/Note.md`
- `<folder-name>/` example directory
- focused `.js` files
- matching `.html` runners

Existing `a.js` files are preserved.

