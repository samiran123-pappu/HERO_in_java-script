let d = new Date(); d.setDate(d.getDate() + 7); // Changes the original object
console.log(d); // Output: Current date + 7 days

// Temporal was created to solve these issues with clearer object types and predictable behavior.

