let x = 10;
let y = 2;

let assigned = x;         // =
let added = x + y;        // +
let multiplied = x * y;   // *
let compared = x > y;     // >

let text = "Hello" + " " + "JS";
let mix = 5 + "5";

console.log({ assigned, added, multiplied, compared, text, mix });
