# CONDITION Folder

This section now includes structured topics:

1. JavaScript Conditionals
2. JavaScript if
3. JavaScript if else
4. JavaScript Ternary Operator
5. JavaScript Switch Statement
6. JavaScript Booleans
7. JavaScript Logical Operators

Each topic has:
- `md/Note.md` for concept notes
- `<TopicName>/` folder with focused `.js` examples
- matching `.html` files for browser runs

## Legacy
- Legacy content is preserved in `OLD/`.
- Use the `JavaScript ...` folders as the primary/current structure.
