# SWITCH CASE

Use `switch` when one expression is compared with many exact values.

## VIP Points
- `break` avoids unwanted fall-through.
- `default` is fallback case.
- `switch` compares with strict equality (`===`).
