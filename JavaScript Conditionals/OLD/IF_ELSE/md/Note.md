# IF ELSE

This OLD section now has detailed examples for:
- `if`
- `if...else`
- `if...else if...else`
- nested conditions

## VIP Points
- Use `if` for one condition.
- Use `if...else` for two outcomes.
- Use `else if` for multiple conditions.
- Prefer clean readable conditions.
